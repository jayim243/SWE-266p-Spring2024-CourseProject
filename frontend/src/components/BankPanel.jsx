import {} from "./utils";

const Bank = ({ title, leftButton, rightButton, mainContent }) => {
  return (
    <div className="bank-panel">
      <div>placeholder</div>
    </div>
  );
};

export default Bank;
